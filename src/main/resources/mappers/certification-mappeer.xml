<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="certificationMapper">

	<select id="signIn" parameterType="string" resultType="user">
		SELECT *
		FROM USERS
		WHERE USER_ID = #{userId}
	</select>

	<insert id="signUp" parameterType="user">
		INSERT INTO USERS(user_id, user_pw, name, serial_num_l, serial_num_f, phone, height, weight, profileImg)
		VALUES (
		        #{userId},
                #{userPw},
                #{name},
                #{serialNumL},
                #{serialNumF},
                #{phone},
                #{height},
                #{weight},
                #{profileImg}
			   )
	</insert>
</mapper>